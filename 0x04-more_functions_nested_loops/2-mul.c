#include "main.h"

/**
 * mul -Multiplies two intergers
 * @x: the first numbber to multipply
 * @y: the second number to multiply with first number
 * Return: The result of multiplicatin between x param and y parm
 */

int mul(int x, int y)

{
	return (x * y);
}
